export const REFERRAL_RATE = 10

export const DEFAULT_EXTRA_XRPL = 0.1
export const XRPL_RESERVE_AMOUNT = 1;
export const LIMIT_REST_XRP_BALANCE = 0.01;

// Configuration

export enum ResultCode {
	SUCCESS = 0,
	INTERNAL,
	PARAMETER,
	USER_INSUFFICIENT_SOL,
	USER_INSUFFICIENT_JITO_FEE_SOL,
	USER_INSUFFICIENT_ENOUGH_SOL,
	INVALIDE_USER,
	INVALIDE_TOKEN,
}

export const whitelist: string[] = [

]
